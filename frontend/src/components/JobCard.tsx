export default function JobCard({ job }: { job: any }) { return (<div className="rounded-2xl border border-border bg-surface px-4 py-3"><div className="flex flex-col gap-1 sm:flex-row sm:items-center sm:justify-between"><div className="font-medium text-text">{job.title || "未命名岗位"}</div><div className="text-sm text-muted">{job.salary_raw || job.salary || "—"}</div></div><div className="mt-1 text-sm text-muted">{job.company || ""}{job.company && job.city ? " · " : ""}{job.city || ""}</div></div>) }
